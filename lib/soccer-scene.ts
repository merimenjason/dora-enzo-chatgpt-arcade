import {SoccerGame} from './soccer-game';
export function drawSoccer(c:CanvasRenderingContext2D,g:SoccerGame,t:number){c.clearRect(0,0,1060,660);c.fillStyle='#243b44';c.fillRect(0,0,1060,660);for(let i=0;i<100;i++){c.fillStyle=i%3?'#a8bac0':'#d9bb86';c.fillRect(i*11,8,5,6);c.fillRect(i*11,647,5,6)}c.save();c.translate(30,30);c.fillStyle='#4d865b';c.fillRect(0,0,1000,600);for(let i=0;i<10;i+=2){c.fillStyle='#559164';c.fillRect(i*100,0,100,600)}
 c.strokeStyle='#e1edc9';c.lineWidth=2;c.strokeRect(4,4,992,592);c.beginPath();c.moveTo(500,4);c.lineTo(500,596);c.moveTo(580,300);c.arc(500,300,80,0,Math.PI*2);c.stroke();c.beginPath();c.arc(500,300,3,0,Math.PI*2);c.fillStyle='#e1edc9';c.fill();
 for(const x of [0,850])c.strokeRect(x,160,150,280);for(const x of [0,950])c.strokeRect(x,230,50,140);for(const x of [-24,1000]){c.fillStyle='#b6c3bd';c.fillRect(x,230,24,140);c.strokeStyle='#e6eadb';c.lineWidth=1;for(let y=230;y<=370;y+=14){c.beginPath();c.moveTo(x,y);c.lineTo(x+24,y);c.stroke()}c.strokeRect(x,230,24,140)}
 // Shooting guide only while the selected player controls the ball.
 if(g.owner===g.active&&g.state==='playing'){const p=g.players[g.active],dx=g.aim.x-p.x,dy=g.aim.y-p.y,d=Math.hypot(dx,dy)||1;c.save();c.setLineDash([5,8]);c.strokeStyle='#ffe1a188';c.beginPath();c.moveTo(p.x*10,p.y*10);c.lineTo((p.x+dx/d*12)*10,(p.y+dy/d*12)*10);c.stroke();c.restore()}
 for(const p of g.players){const x=p.x*10,y=p.y*10,blue=p.team===0,active=p.id===g.active;c.save();c.translate(x,y);c.fillStyle='#173a3655';c.beginPath();c.ellipse(0,11,17,7,0,0,Math.PI*2);c.fill();if(active){c.strokeStyle='#ffe1a3';c.lineWidth=2.5;c.beginPath();c.ellipse(0,6,21,15,0,0,Math.PI*2);c.stroke();c.fillStyle='#ffe1a3';c.beginPath();c.moveTo(-5,-35);c.lineTo(5,-35);c.lineTo(0,-28);c.fill()}
 const coat=blue?'#eee7d9':'#727883',ear=blue?'#cca69e':'#a89599';c.fillStyle=coat;c.beginPath();c.ellipse(-14,7,13,6,-.3,0,Math.PI*2);c.fill();for(const sign of [-1,1]){c.fillStyle=coat;c.beginPath();c.ellipse(sign*9,-17,6,12,sign*.3,0,Math.PI*2);c.fill();c.fillStyle=ear;c.beginPath();c.ellipse(sign*9,-19,3,7,sign*.3,0,Math.PI*2);c.fill()}
 c.fillStyle=coat;c.beginPath();c.ellipse(0,-3,14,16,0,0,Math.PI*2);c.fill();c.fillStyle=p.keeper?'#e6c960':blue?'#77bedd':'#df9270';c.beginPath();c.ellipse(0,6,14,9,0,0,Math.PI*2);c.fill();c.fillStyle='#26333b';for(const sign of [-1,1]){c.beginPath();c.arc(sign*5,-8,2,0,Math.PI*2);c.fill()}c.fillStyle='#b88d88';c.beginPath();c.arc(0,-2,2,0,Math.PI*2);c.fill();c.fillStyle='#fff5dd';c.font='bold 9px Arial';c.textAlign='center';c.fillText(p.keeper?'G':String(p.id%4+1),0,9);c.font='bold 11px Arial';c.fillStyle=active?'#ffe7af':'#eff3df';c.shadowColor='#173a38';c.shadowBlur=3;c.fillText(p.name,0,31);c.restore()}
 const bx=g.ball.x*10,by=g.ball.y*10;c.fillStyle='#17352e77';c.beginPath();c.ellipse(bx,by+5,6,3,0,0,Math.PI*2);c.fill();c.fillStyle='#fff8e5';c.beginPath();c.arc(bx,by,6,0,Math.PI*2);c.fill();c.fillStyle='#37464c';for(let i=0;i<3;i++){const a=t*4+i*2.1;c.beginPath();c.arc(bx+Math.cos(a)*3,by+Math.sin(a)*3,1.7,0,Math.PI*2);c.fill()}c.restore()}
