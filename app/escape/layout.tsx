import type {Metadata} from 'next';
export const metadata:Metadata={title:'Enzo and Dora Escapes from ICE',description:'A 3D chinchilla spy escape with cage puzzles, disguises, intel and playful gadgets.'};
export default function EscapeLayout({children}:{children:React.ReactNode}){return children}
